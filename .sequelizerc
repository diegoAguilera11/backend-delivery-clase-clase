const path = require('path');

module.exports = {
  'config': path.resolve('./database/config', 'config.js'), // Ruta al archivo de configuración
  'models-path': path.resolve('./database/models'), // Ruta a la carpeta de modelos
  'migrations-path': path.resolve('./database/migrations'), // Ruta a la carpeta de migraciones
  'seeders-path': path.resolve('./database/seeders') // Ruta a la carpeta de seeders
};
